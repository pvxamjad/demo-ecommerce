{
    "builds": [{
        "src": "ecom/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "ecom/wsgi.py"
        }
    ]
}